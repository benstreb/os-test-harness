- header: compare.h

- function: compare
  type: char, char* -> int
  tests:
    - args: ['a', ['a', 'b']]
      less_than: 0
    - args: [0, !zeroed 3]
      equals: 0
    - data:
        array: ['c', 'b', 'a']
      args: ['b', !ptr region+1]
      greater_than: 0
